import os
import z5py


def merge_projects(project_a, project_b, new_project, strategy=''):
    """ Merge to paintera projects.

    Args:
        project_a (str): path to first project folder
        project_b (str): path to second project folder
        new_project (str): path to output project folder
        strategy (str): merge strategy TODO
    """
    pass
